import styled from 'styled-components';

const StyledTokenHolder = styled.div`
  height: auto;
  width: 90px;
  margin: 0.5em;
  padding: 0.5em;
  background: black;
  border: 1px solid red;
`

export default StyledTokenHolder;